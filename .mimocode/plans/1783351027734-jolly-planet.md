# Plan: Fix Bugs + UI Improvements + Floating Side Nav

## 1. Fix Critical Bugs

### `templates/base.html`
- Fix CSS path: `static/css/style.css` → `static/core/css/style.css`
- Move `<script src="js/favicon.js">` inside `</body>` (currently after `</html>`)

### `templates/components/header.html`
- Fix GitHub URL: `yourusername` → `fatiimahoseini`
- Change dark mode toggle icon to sun icon (site is dark, so toggle shows "switch to light")
- Add mobile hamburger menu

## 2. Fix Skills Scroller

### `core/templates/core/home.html`
- Make both skill lists identical so the `translateX(-50%)` loop works seamlessly
- Use all unique skills in one combined list, duplicated

## 3. Newsletter — Collect Emails

### `blog/models.py`
- Add `Subscriber` model with `email` (unique) and `subscribed_at`

### `blog/admin.py`
- Register `Subscriber` in admin

### `blog/views.py`
- Add `newsletter_subscribe` view — POST handler, saves email, redirects back

### `blog/urls.py`
- Add `path("subscribe/", views.newsletter_subscribe, name="newsletter_subscribe")`

### `blog/templates/blog/blog.html`
- Update form: `action="{% url 'newsletter_subscribe' %}" method="POST"` with `{% csrf_token %}`

## 4. Mobile Hamburger Menu

### `templates/components/header.html`
- Add hamburger button (hidden on md+) and mobile menu drawer
- Mobile menu: slide-down panel with Home, Blog, Contact links
- Simple JS toggle in a `<script>` tag at bottom of header

## 5. Floating Side Nav (New Feature)

A compact vertical nav that appears on the left side after scrolling past the hero section. Icons only, sticky, moves with scroll.

### `templates/components/floating-nav.html` (new file)
- Fixed position left side, vertically centered
- 3 icons: Home (house), Blog (pen/document), Contact (envelope)
- Hidden initially, appears after scrolling past hero
- Uses `IntersectionObserver` or scroll event to detect hero position
- Compact: `w-10 h-10` icon buttons, dark background with border

### `templates/base.html`
- Include the floating nav component

### Files
- `templates/components/floating-nav.html` (new)
- `templates/base.html` — add include

## Files to Modify
- `templates/base.html`
- `templates/components/header.html`
- `templates/components/floating-nav.html` (new)
- `core/templates/core/home.html`
- `blog/models.py`
- `blog/admin.py`
- `blog/views.py`
- `blog/urls.py`
- `blog/templates/blog/blog.html`

## Verification
- Run `python manage.py makemigrations blog && python manage.py migrate`
- Run `python manage.py check`
- Visit `/` — skills scroller loops smoothly, floating nav appears after hero
- Visit `/blog/` — newsletter form submits, hamburger menu works on mobile
- Visit `/contact/` — page loads correctly
- Check all pages have working favicon script and correct CSS
